<Window x:Class="WorkProgressManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkProgressManagement"
        xmlns:views="clr-namespace:WorkProgressManagement.Views.UserControls"
        xmlns:pages="clr-namespace:WorkProgressManagement.Views.Pages"
        xmlns:vm="clr-namespace:WorkProgressManagement.ViewModels"
        mc:Ignorable="d"
        Title="Work Progress Management"
        Height="800"
        Width="1400"
        MinHeight="700"
        MinWidth="1200"
        WindowStartupLocation="CenterScreen">

	<Window.DataContext>
		<vm:MainViewModel/>
	</Window.DataContext>

	<Window.Resources>
		<DataTemplate DataType="{x:Type vm:DashboardViewModel}">
			<pages:DashboardPage/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vm:ProjectsViewModel}">
			<pages:ProjectsPage/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vm:TasksViewModel}">
			<pages:TasksPage/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vm:UsersViewModel}">
			<pages:UsersPage/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vm:ReportsViewModel}">
			<pages:ReportsPage/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type vm:ProfileViewModel}">
			<pages:ProfilePage/>
		</DataTemplate>
	</Window.Resources>

	<Grid Background="{StaticResource BackgroundBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="240"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<Border Grid.Column="0"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,1,0">
			<views:SidebarControl/>
		</Border>

		<Grid Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="70"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<Border Grid.Row="0"
                    Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1">
				<views:TopBarControl/>
			</Border>

			<ContentControl x:Name="MainContent"
                            Grid.Row="1"
                            Margin="20"
                            Content="{Binding CurrentView}"/>
		</Grid>
	</Grid>
</Window>